<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.example.HotelMapper"> 
    <select id="selectHotelById" resultType="Hotel">
        SELECT * FROM hotel WHERE city = #{cityId}
    </select>
</mapper>